
load @tunebond/snow/base/native-integer
  take form native-integer

load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/privateca/v1beta1/base/list-certificate-authorities-response
  take form list-certificate-authorities-response

boot privateca-projects-locations-certificate-authorities-list, name <privateca.projects.locations.certificateAuthorities.list>
  deed get
  note <Lists CertificateAuthorities.>

  take parent
    like native-string
    note <Required. The resource name of the location associated with the CertificateAuthorities, in the format `projects/*/locations/*`.>
  take filter
    like native-string
    note <Optional. Only include resources that match the filter in the response.>
    void take
  take order-by
    like native-string
    note <Optional. Specify how the results should be sorted.>
    void take
  take page-size
    like native-integer
    note <Optional. Limit on the number of CertificateAuthorities to include in the response. Further CertificateAuthorities can subsequently be obtained by including the ListCertificateAuthoritiesResponse.next_page_token in a subsequent request. If unspecified, the server will pick an appropriate default.>
    void take
  take page-token
    like native-string
    note <Optional. Pagination token, returned earlier via ListCertificateAuthoritiesResponse.next_page_token.>
    void take

  line </v1beta1/{parent}/certificateAuthorities>

  find <filter>, loan filter
  find <orderBy>, loan order-by
  find <pageSize>, loan page-size
  find <pageToken>, loan page-token

  loot 200
    seed json, like list-certificate-authorities-response
    note <Successful response>