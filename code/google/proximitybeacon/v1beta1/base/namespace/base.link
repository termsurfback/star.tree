
form namespace, name <Namespace>
  note <An attachment namespace defines read and write access for all the attachments created under it. Each namespace is globally unique, and owned by one project which is the only project that can create attachments under it.>
  take namespace-name, name <namespaceName>
    void take
  take serving-visibility, name <servingVisibility>
    void take