
load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/run/v1/base/service
  take form service

boot run-projects-locations-services-get, name <run.projects.locations.services.get>
  deed get
  note <Get information about a service.>

  take name
    like native-string
    note <The name of the service to retrieve. For Cloud Run (fully managed), replace {namespace_id} with the project ID or number.>

  line </v1/{name}>

  loot 200
    seed json, like service
    note <Successful response>
