
load @tunebond/snow/base/native-integer
  take form native-integer

load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/connectors/v1/base/list-connections-response
  take form list-connections-response

boot connectors-projects-locations-connections-list, name <connectors.projects.locations.connections.list>
  deed get
  note <Lists Connections in a given project and location.>

  take parent
    like native-string
    note <Required. Parent resource of the Connection, of the form: `projects/*/locations/*`>
  take filter
    like native-string
    note <Filter.>
    void take
  take order-by
    like native-string
    note <Order by parameters.>
    void take
  take page-size
    like native-integer
    note <Page size.>
    void take
  take page-token
    like native-string
    note <Page token.>
    void take
  take view
    like native-string
    note <Specifies which fields of the Connection are returned in the response. Defaults to `BASIC` view.>
    void take

  line </v1/{parent}/connections>

  find <filter>, loan filter
  find <orderBy>, loan order-by
  find <pageSize>, loan page-size
  find <pageToken>, loan page-token
  find <view>, loan view

  loot 200
    seed json, like list-connections-response
    note <Successful response>