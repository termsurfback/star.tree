
form get-vulnz-occurrences-summary-response, name <GetVulnzOccurrencesSummaryResponse>
  note <A summary of how many vulnz occurrences there are per severity type. counts by groups, or if we should have different summary messages like this.>
  take counts, name <counts>
    void take