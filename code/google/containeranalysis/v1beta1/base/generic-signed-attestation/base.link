
form generic-signed-attestation, name <GenericSignedAttestation>
  note <An attestation wrapper that uses the Grafeas `Signature` message. This attestation must define the `serialized_payload` that the `signatures` verify and any metadata necessary to interpret that plaintext. The signatures should always be over the `serialized_payload` bytestring.>
  take content-type, name <contentType>
    void take
  take serialized-payload, name <serializedPayload>
    void take
  take signatures, name <signatures>
    void take