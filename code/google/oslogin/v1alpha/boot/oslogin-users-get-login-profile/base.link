
load @treesurf/snow/base/native-string
  take form native-string

load @treesurf/snow/googleapis.com/oslogin/v1alpha/base/login-profile
  take form login-profile

boot oslogin-users-get-login-profile, name <oslogin.users.getLoginProfile>
  deed get
  note <Retrieves the profile information used for logging in to a virtual machine on Google Compute Engine.>

  take name
    like native-string
    note <Required. The unique ID for the user in format `users/{user}`.>
  take operating-system-type
    like native-string
    note <The type of operating system associated with the account.>
    void take
  take project-id
    like native-string
    note <The project ID of the Google Cloud Platform project.>
    void take
  take system-id
    like native-string
    note <A system ID for filtering the results of the request.>
    void take
  take view
    like native-string
    note <The view configures whether to retrieve security keys information.>
    void take

  line </v1alpha/{name}/loginProfile>

  find <operatingSystemType>, loan operating-system-type
  find <projectId>, loan project-id
  find <systemId>, loan system-id
  find <view>, loan view

  loot 200
    seed json, like login-profile
    note <Successful response>