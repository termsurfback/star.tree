
load @nerdbond/snow/base/native-integer
  take form native-integer

load @nerdbond/snow/base/native-string
  take form native-string

load @nerdbond/snow/googleapis.com/content/v2.1/base/accounttax-list-response
  take form accounttax-list-response

boot content-accounttax-list, name <content.accounttax.list>
  deed get
  note <Lists the tax settings of the sub-accounts in your Merchant Center account.>

  take merchant-id
    like native-string
    note <The ID of the managing account. This must be a multi-client account.>
  take max-results
    like native-integer
    note <The maximum number of tax settings to return in the response, used for paging.>
    void take
  take page-token
    like native-string
    note <The token returned by the previous request.>
    void take

  line </{merchant-id}/accounttax>

  find <maxResults>, loan max-results
  find <pageToken>, loan page-token

  loot 200
    seed json, like accounttax-list-response
    note <Successful response>