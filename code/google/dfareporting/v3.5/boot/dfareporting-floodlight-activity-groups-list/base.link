
load @drumwork/snow/base/list
  take form list

load @drumwork/snow/base/native-integer
  take form native-integer

load @drumwork/snow/base/native-string
  take form native-string

load @drumwork/snow/googleapis.com/dfareporting/v3.5/base/floodlight-activity-groups-list-response
  take form floodlight-activity-groups-list-response

boot dfareporting-floodlight-activity-groups-list, name <dfareporting.floodlightActivityGroups.list>
  deed get
  note <Retrieves a list of floodlight activity groups, possibly filtered. This method supports paging.>

  take profile-id
    like native-string
    note <User profile ID associated with this request.>
  take advertiser-id
    like native-string
    note <Select only floodlight activity groups with the specified advertiser ID. Must specify either advertiserId or floodlightConfigurationId for a non-empty result.>
    void take
  take floodlight-configuration-id
    like native-string
    note <Select only floodlight activity groups with the specified floodlight configuration ID. Must specify either advertiserId, or floodlightConfigurationId for a non-empty result.>
    void take
  take ids
    like list
      like native-string
    note <Select only floodlight activity groups with the specified IDs. Must specify either advertiserId or floodlightConfigurationId for a non-empty result.>
    void take
  take max-results
    like native-integer
    note <Maximum number of results to return.>
    void take
  take page-token
    like native-string
    note <Value of the nextPageToken from the previous result page.>
    void take
  take search-string
    like native-string
    note <Allows searching for objects by name or ID. Wildcards (*) are allowed. For example, "floodlightactivitygroup*2015" will return objects with names like "floodlightactivitygroup June 2015", "floodlightactivitygroup April 2015", or simply "floodlightactivitygroup 2015". Most of the searches also add wildcards implicitly at the start and the end of the search string. For example, a search string of "floodlightactivitygroup" will match objects with name "my floodlightactivitygroup activity", "floodlightactivitygroup 2015", or simply "floodlightactivitygroup".>
    void take
  take sort-field
    like native-string
    note <Field by which to sort the list.>
    void take
  take sort-order
    like native-string
    note <Order of sorted results.>
    void take
  take type
    like native-string
    note <Select only floodlight activity groups with the specified floodlight activity group type.>
    void take

  line </dfareporting/v3.5/userprofiles/{profile-id}/floodlightActivityGroups>

  find <advertiserId>, loan advertiser-id
  find <floodlightConfigurationId>, loan floodlight-configuration-id
  find <ids>, loan ids
  find <maxResults>, loan max-results
  find <pageToken>, loan page-token
  find <searchString>, loan search-string
  find <sortField>, loan sort-field
  find <sortOrder>, loan sort-order
  find <type>, loan type

  loot 200
    seed json, like floodlight-activity-groups-list-response
    note <Successful response>