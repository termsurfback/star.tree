
form proxy-info, name <ProxyInfo>
  note <Configuration info for an HTTP proxy. For a direct proxy, set the host, port, and excluded_hosts fields. For a PAC script proxy, set the pac_uri field.>
  take excluded-hosts, name <excludedHosts>
    void take
  take host, name <host>
    void take
  take pac-uri, name <pacUri>
    void take
  take port, name <port>
    void take