
load @textsurf/snow/base/native-integer
  take form native-integer

load @textsurf/snow/base/native-string
  take form native-string

load @textsurf/snow/googleapis.com/cloudchannel/v1/base/google-cloud-channel-v1-list-products-response
  take form google-cloud-channel-v1-list-products-response

boot cloudchannel-products-list, name <cloudchannel.products.list>
  deed get
  note <Lists the Products the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Required request parameters are missing or invalid.>

  take account
    like native-string
    note <Required. The resource name of the reseller account. Format: accounts/{account_id}.>
    void take
  take language-code
    like native-string
    note <Optional. The BCP-47 language code. For example, "en-US". The response will localize in the corresponding language code, if specified. The default value is "en-US".>
    void take
  take page-size
    like native-integer
    note <Optional. Requested page size. Server might return fewer results than requested. If unspecified, returns at most 100 Products. The maximum value is 1000; the server will coerce values above 1000.>
    void take
  take page-token
    like native-string
    note <Optional. A token for a page of results other than the first page.>
    void take

  line </v1/products>

  find <account>, loan account
  find <languageCode>, loan language-code
  find <pageSize>, loan page-size
  find <pageToken>, loan page-token

  loot 200
    seed json, like google-cloud-channel-v1-list-products-response
    note <Successful response>
