
load @drumwork/snow/base/native-string
  take form native-string

load @drumwork/snow/googleapis.com/admin/directory_v1/base/calendar-resource
  take form calendar-resource

boot directory-resources-calendars-update, name <directory.resources.calendars.update>
  note <Updates a calendar resource. This method supports patch semantics, meaning you only need to include the fields you wish to update. Fields that are not present in the request will be preserved.>

  take customer
    like native-string
    note <The unique ID for the customer's Google Workspace account. As an account administrator, you can also use the `my_customer` alias to represent your account's customer ID.>
  take calendar-resource-id
    like native-string
    note <The unique ID of the calendar resource to update.>
  take calendar-resource, like calendar-resource
    void take

  line </admin/directory/v1/customer/{customer}/resources/calendars/{calendar-resource-id}>

  seed json, loan calendar-resource

  loot 200
    seed json, like calendar-resource
    note <Successful response>