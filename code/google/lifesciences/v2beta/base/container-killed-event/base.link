
form container-killed-event, name <ContainerKilledEvent>
  note <An event generated when a container is forcibly terminated by the worker. Currently, this only occurs when the container outlives the timeout specified by the user.>
  take action-id, name <actionId>
    void take