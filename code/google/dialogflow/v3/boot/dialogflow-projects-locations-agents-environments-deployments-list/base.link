
load @wavebond/snow/base/native-integer
  take form native-integer

load @wavebond/snow/base/native-string
  take form native-string

load @wavebond/snow/googleapis.com/dialogflow/v3/base/google-cloud-dialogflow-cx-v3-list-deployments-response
  take form google-cloud-dialogflow-cx-v3-list-deployments-response

boot dialogflow-projects-locations-agents-environments-deployments-list, name <dialogflow.projects.locations.agents.environments.deployments.list>
  deed get
  note <Returns the list of all deployments in the specified Environment.>

  take parent
    like native-string
    note <Required. The Environment to list all environments for. Format: `projects//locations//agents//environments/`.>
  take page-size
    like native-integer
    note <The maximum number of items to return in a single page. By default 20 and at most 100.>
    void take
  take page-token
    like native-string
    note <The next_page_token value returned from a previous list request.>
    void take

  line </v3/{parent}/deployments>

  find <pageSize>, loan page-size
  find <pageToken>, loan page-token

  loot 200
    seed json, like google-cloud-dialogflow-cx-v3-list-deployments-response
    note <Successful response>