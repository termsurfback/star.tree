
load @tunebond/snow/base/native-integer
  take form native-integer

load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/dialogflow/v2/base/google-cloud-dialogflow-v2-list-answer-records-response
  take form google-cloud-dialogflow-v2-list-answer-records-response

boot dialogflow-projects-locations-answer-records-list, name <dialogflow.projects.locations.answerRecords.list>
  deed get
  note <Returns the list of all answer records in the specified project in reverse chronological order.>

  take parent
    like native-string
    note <Required. The project to list all answer records for in reverse chronological order. Format: `projects//locations/`.>
  take filter
    like native-string
    note <Required. Filters to restrict results to specific answer records. Filter on answer record type. Currently predicates on `type` is supported, valid values are `ARTICLE_ANSWER`, `FAQ_ANSWER`. For more information about filtering, see [API Filtering](https://aip.dev/160).>
    void take
  take page-size
    like native-integer
    note <Optional. The maximum number of records to return in a single page. The server may return fewer records than this. If unspecified, we use 10. The maximum is 100.>
    void take
  take page-token
    like native-string
    note <Optional. The ListAnswerRecordsResponse.next_page_token value returned from a previous list request used to continue listing on the next page.>
    void take

  line </v2/{parent}/answerRecords>

  find <filter>, loan filter
  find <pageSize>, loan page-size
  find <pageToken>, loan page-token

  loot 200
    seed json, like google-cloud-dialogflow-v2-list-answer-records-response
    note <Successful response>