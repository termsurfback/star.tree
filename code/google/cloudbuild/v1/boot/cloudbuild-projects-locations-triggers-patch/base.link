
load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/cloudbuild/v1/base/build-trigger
  take form build-trigger

boot cloudbuild-projects-locations-triggers-patch, name <cloudbuild.projects.locations.triggers.patch>
  deed patch
  note <Updates a `BuildTrigger` by its project ID and trigger ID. This API is experimental.>

  take resource-name
    like native-string
    note <The `Trigger` name with format: `projects/{project}/locations/{location}/triggers/{trigger}`, where {trigger} is a unique identifier generated by the service.>
  take project-id
    like native-string
    note <Required. ID of the project that owns the trigger.>
    void take
  take trigger-id
    like native-string
    note <Required. ID of the `BuildTrigger` to update.>
    void take
  take build-trigger, like build-trigger
    void take

  line </v1/{resource-name}>

  find <projectId>, loan project-id
  find <triggerId>, loan trigger-id

  seed json, loan build-trigger

  loot 200
    seed json, like build-trigger
    note <Successful response>