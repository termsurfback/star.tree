
load @tunebond/snow/googleapis.com/analyticsdata/v1beta/base/date-range
  take form date-range

form cohort, name <Cohort>
  note <Defines a cohort selection criteria. A cohort is a group of users who share a common characteristic. For example, users with the same `firstSessionDate` belong to the same cohort.>
  take date-range, name <dateRange>
    like date-range
    void take
  take dimension, name <dimension>
    void take
  take name, name <name>
    void take