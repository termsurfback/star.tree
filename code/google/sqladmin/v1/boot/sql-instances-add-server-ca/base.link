
load @drumwork/snow/base/native-string
  take form native-string

load @drumwork/snow/googleapis.com/sqladmin/v1/base/operation
  take form operation

boot sql-instances-add-server-ca, name <sql.instances.addServerCa>
  deed post
  note <Adds a new trusted Certificate Authority (CA) version for the specified instance. Required to prepare for a certificate rotation. If a CA version was previously added but never used in a certificate rotation, this operation replaces that version. There cannot be more than one CA version waiting to be rotated in.>

  take project
    like native-string
    note <Project ID of the project that contains the instance.>
  take instance
    like native-string
    note <Cloud SQL instance ID. This does not include the project ID.>

  line </v1/projects/{project}/instances/{instance}/addServerCa>

  loot 200
    seed json, like operation
    note <Successful response>