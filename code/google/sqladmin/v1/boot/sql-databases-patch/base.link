
load @nerdbond/snow/base/native-string
  take form native-string

load @nerdbond/snow/googleapis.com/sqladmin/v1/base/operation
  take form operation

boot sql-databases-patch, name <sql.databases.patch>
  deed patch
  note <Partially updates a resource containing information about a database inside a Cloud SQL instance. This method supports patch semantics.>

  take project
    like native-string
    note <Project ID of the project that contains the instance.>
  take instance
    like native-string
    note <Database instance ID. This does not include the project ID.>
  take database
    like native-string
    note <Name of the database to be updated in the instance.>
  take database, like database
    void take

  line </v1/projects/{project}/instances/{instance}/databases/{database}>

  seed json, loan database

  loot 200
    seed json, like operation
    note <Successful response>