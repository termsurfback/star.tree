
form redirect, name <Redirect>
  note <A [`Redirect`](https://firebase.google.com/docs/hosting/full-config#redirects) specifies a URL pattern that, if matched to the request URL path, triggers Hosting to respond with a redirect to the specified destination path.>
  take glob, name <glob>
    void take
  take location, name <location>
    void take
  take regex, name <regex>
    void take
  take status-code, name <statusCode>
    void take