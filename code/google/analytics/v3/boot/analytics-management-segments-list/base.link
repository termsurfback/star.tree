
load @drumwork/snow/base/native-integer
  take form native-integer

load @drumwork/snow/googleapis.com/analytics/v3/base/segments
  take form segments

boot analytics-management-segments-list, name <analytics.management.segments.list>
  note <Lists segments to which the user has access.>

  take max-results
    like native-integer
    note <The maximum number of segments to include in this response.>
    void take
  take start-index
    like native-integer
    note <An index of the first segment to retrieve. Use this parameter as a pagination mechanism along with the max-results parameter.>
    void take

  line </management/segments>

  find <max-results>, loan max-results
  find <start-index>, loan start-index

  loot 200
    seed json, like segments
    note <Successful response>