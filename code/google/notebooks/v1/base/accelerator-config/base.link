
form accelerator-config, name <AcceleratorConfig>
  note <Definition of a hardware accelerator. Note that not all combinations of `type` and `core_count` are valid. Check [GPUs on Compute Engine](/compute/docs/gpus/#gpus-list) to find a valid combination. TPUs are not supported.>
  take core-count, name <coreCount>
    void take
  take type, name <type>
    void take