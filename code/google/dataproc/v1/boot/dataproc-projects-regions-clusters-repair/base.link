
load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/dataproc/v1/base/operation
  take form operation

boot dataproc-projects-regions-clusters-repair, name <dataproc.projects.regions.clusters.repair>
  deed post
  note <Repairs a cluster.>

  take project-id
    like native-string
    note <Required. The ID of the Google Cloud Platform project the cluster belongs to.>
  take region
    like native-string
    note <Required. The Dataproc region in which to handle the request.>
  take cluster-name
    like native-string
    note <Required. The cluster name.>
  take repair-cluster-request, like repair-cluster-request
    void take

  line </v1/projects/{project-id}/regions/{region}/clusters/{cluster-name}:repair>

  seed json, loan repair-cluster-request

  loot 200
    seed json, like operation
    note <Successful response>
