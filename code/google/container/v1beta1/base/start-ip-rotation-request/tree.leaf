
form start-ip-rotation-request, name <StartIPRotationRequest>
  note <StartIPRotationRequest creates a new IP for the cluster and then performs a node upgrade on each node pool to point to the new IP.>
  take cluster-id, name <clusterId>
    void take
  take name, name <name>
    void take
  take project-id, name <projectId>
    void take
  take rotate-credentials, name <rotateCredentials>
    void take
  take zone, name <zone>
    void take