
load @tunebond/snow/base/native-boolean
  take form native-boolean

load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/appengine/v1alpha/base/operation
  take form operation

boot appengine-apps-domain-mappings-create, name <appengine.apps.domainMappings.create>
  deed post
  note <Maps a domain to an application. A user must be authorized to administer a domain in order to map it to an application. For a list of available authorized domains, see AuthorizedDomains.ListAuthorizedDomains.>

  take apps-id
    like native-string
    note <Part of `parent`. Name of the parent Application resource. Example: apps/myapp.>
  take no-managed-certificate
    like native-boolean
    note <Whether a managed certificate should be provided by App Engine. If true, a certificate ID must be manaually set in the DomainMapping resource to configure SSL for this domain. If false, a managed certificate will be provisioned and a certificate ID will be automatically populated.>
    void take
  take override-strategy
    like native-string
    note <Whether the domain creation should override any existing mappings for this domain. By default, overrides are rejected.>
    void take
  take domain-mapping, like domain-mapping
    void take

  line </v1alpha/apps/{apps-id}/domainMappings>

  find <noManagedCertificate>, loan no-managed-certificate
  find <overrideStrategy>, loan override-strategy

  seed json, loan domain-mapping

  loot 200
    seed json, like operation
    note <Successful response>