
load @textsurf/snow/base/native-integer
  take form native-integer

load @textsurf/snow/base/native-string
  take form native-string

load @textsurf/snow/googleapis.com/monitoring/v3/base/list-uptime-check-configs-response
  take form list-uptime-check-configs-response

boot monitoring-projects-uptime-check-configs-list, name <monitoring.projects.uptimeCheckConfigs.list>
  deed get
  note <Lists the existing valid Uptime check configurations for the project (leaving out any invalid configurations).>

  take parent
    like native-string
    note <Required. The project (https://cloud.google.com/monitoring/api/v3#project_name) whose Uptime check configurations are listed. The format is: projects/[PROJECT_ID_OR_NUMBER]>
  take page-size
    like native-integer
    note <The maximum number of results to return in a single response. The server may further constrain the maximum number of results returned in a single page. If the page_size is &lt;=0, the server will decide the number of results to be returned.>
    void take
  take page-token
    like native-string
    note <If this field is not empty then it must contain the nextPageToken value returned by a previous call to this method. Using this field causes the method to return more results from the previous method call.>
    void take

  line </v3/{parent}/uptimeCheckConfigs>

  find <pageSize>, loan page-size
  find <pageToken>, loan page-token

  loot 200
    seed json, like list-uptime-check-configs-response
    note <Successful response>
