
form mosaic-layout, name <MosaicLayout>
  note <A mosaic layout divides the available space into a grid of blocks, and overlays the grid with tiles. Unlike GridLayout, tiles may span multiple grid blocks and can be placed at arbitrary locations in the grid.>
  take columns, name <columns>
    void take
  take tiles, name <tiles>
    void take