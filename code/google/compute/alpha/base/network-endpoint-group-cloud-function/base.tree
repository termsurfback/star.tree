
form network-endpoint-group-cloud-function, name <NetworkEndpointGroupCloudFunction>
  note <Configuration for a Cloud Function network endpoint group (NEG). The function must be provided explicitly or in the URL mask. Note: Cloud Function must be in the same project and located in the same region as the Serverless NEG.>
  take function, name <function>
    void take
  take url-mask, name <urlMask>
    void take