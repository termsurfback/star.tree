
form upcoming-maintenance-time-window, name <UpcomingMaintenanceTimeWindow>
  note <Represents a window of time using two timestamps: `earliest` and `latest`. This timestamp values are in RFC3339 text format.>
  take earliest, name <earliest>
    void take
  take latest, name <latest>
    void take