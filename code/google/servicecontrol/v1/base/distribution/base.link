
load @nerdbond/snow/googleapis.com/servicecontrol/v1/base/explicit-buckets
  take form explicit-buckets

load @nerdbond/snow/googleapis.com/servicecontrol/v1/base/exponential-buckets
  take form exponential-buckets

load @nerdbond/snow/googleapis.com/servicecontrol/v1/base/linear-buckets
  take form linear-buckets

form distribution, name <Distribution>
  note <Distribution represents a frequency distribution of double-valued sample points. It contains the size of the population of sample points plus additional optional information: * the arithmetic mean of the samples * the minimum and maximum of the samples * the sum-squared-deviation of the samples, used to compute variance * a histogram of the values of the sample points>
  take bucket-counts, name <bucketCounts>
    void take
  take count, name <count>
    void take
  take exemplars, name <exemplars>
    void take
  take explicit-buckets, name <explicitBuckets>
    like explicit-buckets
    void take
  take exponential-buckets, name <exponentialBuckets>
    like exponential-buckets
    void take
  take linear-buckets, name <linearBuckets>
    like linear-buckets
    void take
  take maximum, name <maximum>
    void take
  take mean, name <mean>
    void take
  take minimum, name <minimum>
    void take
  take sum-of-squared-deviation, name <sumOfSquaredDeviation>
    void take