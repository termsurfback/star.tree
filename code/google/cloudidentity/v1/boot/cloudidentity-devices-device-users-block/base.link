
load @wavebond/snow/base/native-string
  take form native-string

load @wavebond/snow/googleapis.com/cloudidentity/v1/base/operation
  take form operation

boot cloudidentity-devices-device-users-block, name <cloudidentity.devices.deviceUsers.block>
  deed post
  note <Blocks device from accessing user data>

  take name
    like native-string
    note <Required. [Resource name](https://cloud.google.com/apis/design/resource_names) of the Device in format: `devices/{device}/deviceUsers/{device_user}`, where device is the unique ID assigned to the Device, and device_user is the unique ID assigned to the User.>
  take google-apps-cloudidentity-devices-v1-block-device-user-request, like google-apps-cloudidentity-devices-v1-block-device-user-request
    void take

  line </v1/{name}:block>

  seed json, loan google-apps-cloudidentity-devices-v1-block-device-user-request

  loot 200
    seed json, like operation
    note <Successful response>