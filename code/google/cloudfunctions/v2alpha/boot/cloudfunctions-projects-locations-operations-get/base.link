
load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/cloudfunctions/v2alpha/base/operation
  take form operation

boot cloudfunctions-projects-locations-operations-get, name <cloudfunctions.projects.locations.operations.get>
  deed get
  note <Gets the latest state of a long-running operation. Clients can use this method to poll the operation result at intervals as recommended by the API service.>

  take name
    like native-string
    note <The name of the operation resource.>

  line </v2alpha/{name}>

  loot 200
    seed json, like operation
    note <Successful response>