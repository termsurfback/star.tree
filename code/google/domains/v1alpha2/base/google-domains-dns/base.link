
form google-domains-dns, name <GoogleDomainsDns>
  note <Configuration for using the free DNS zone provided by Google Domains as a `Registration`'s `dns_provider`. You cannot configure the DNS zone itself using the API. To configure the DNS zone, go to [Google Domains](https://domains.google/).>
  take ds-records, name <dsRecords>
    void take
  take ds-state, name <dsState>
    void take
  take name-servers, name <nameServers>
    void take