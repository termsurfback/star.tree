
form ds-record, name <DsRecord>
  note <Defines a Delegation Signer (DS) record, which is needed to enable DNSSEC for a domain. It contains a digest (hash) of a DNSKEY record that must be present in the domain's DNS zone.>
  take algorithm, name <algorithm>
    void take
  take digest, name <digest>
    void take
  take digest-type, name <digestType>
    void take
  take key-tag, name <keyTag>
    void take