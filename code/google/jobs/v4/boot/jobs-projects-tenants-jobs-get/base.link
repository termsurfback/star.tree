
load @textsurf/snow/base/native-string
  take form native-string

load @textsurf/snow/googleapis.com/jobs/v4/base/job
  take form job

boot jobs-projects-tenants-jobs-get, name <jobs.projects.tenants.jobs.get>
  deed get
  note <Retrieves the specified job, whose status is OPEN or recently EXPIRED within the last 90 days.>

  take name
    like native-string
    note <Required. The resource name of the job to retrieve. The format is "projects/{project_id}/tenants/{tenant_id}/jobs/{job_id}". For example, "projects/foo/tenants/bar/jobs/baz".>

  line </v4/{name}>

  loot 200
    seed json, like job
    note <Successful response>
