
load @tunebond/snow/base/native-string
  take form native-string

load @tunebond/snow/googleapis.com/jobs/v3/base/job
  take form job

boot jobs-projects-jobs-patch, name <jobs.projects.jobs.patch>
  deed patch
  note <Updates specified job. Typically, updated contents become visible in search results within 10 seconds, but it may take up to 5 minutes.>

  take name
    like native-string
    note <Required during job update. The resource name for the job. This is generated by the service when a job is created. The format is "projects/{project_id}/jobs/{job_id}", for example, "projects/api-test-project/jobs/1234". Use of this field in job queries and API calls is preferred over the use of requisition_id since this value is unique.>
  take update-job-request, like update-job-request
    void take

  line </v3/{name}>

  seed json, loan update-job-request

  loot 200
    seed json, like job
    note <Successful response>
