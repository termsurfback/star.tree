
load @termsurf/snow/base/form
  take form form

load @termsurf/snow/base/list
  take form list

load @termsurf/snow/base/native-integer
  take form native-integer

load @termsurf/snow/base/native-string
  take form native-string

load @termsurf/snow/stripe.com/2020-08-27/base/error
  take form error

load @termsurf/snow/stripe.com/2020-08-27/base/get-subscriptions-body
  take form get-subscriptions-body

boot get-subscriptions, name <GetSubscriptions>
  deed get
  note <By default, returns a list of subscriptions that have not been canceled. In order to list canceled subscriptions, specify `status=canceled`.>

  take collection-method
    like native-string
    note <The collection method of the subscriptions to retrieve. Either `charge_automatically` or `send_invoice`.>
    void take
  take created
    like sink
      like form
        take gt, name <gt>
          void take
        take gte, name <gte>
          void take
        take lt, name <lt>
          void take
        take lte, name <lte>
          void take
      like native-integer
    void take
  take current-period-end
    like sink
      like form
        take gt, name <gt>
          void take
        take gte, name <gte>
          void take
        take lt, name <lt>
          void take
        take lte, name <lte>
          void take
      like native-integer
    void take
  take current-period-start
    like sink
      like form
        take gt, name <gt>
          void take
        take gte, name <gte>
          void take
        take lt, name <lt>
          void take
        take lte, name <lte>
          void take
      like native-integer
    void take
  take customer
    like native-string
    note <The ID of the customer whose subscriptions will be retrieved.>
    void take
  take ending-before
    like native-string
    note <A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.>
    void take
  take expand
    like list
      like native-string
    note <Specifies which fields in the response should be expanded.>
    void take
  take limit
    like native-integer
    note <A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.>
    void take
  take price
    like native-string
    note <Filter for subscriptions that contain this recurring price ID.>
    void take
  take starting-after
    like native-string
    note <A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.>
    void take
  take status
    like native-string
    note <The status of the subscriptions to retrieve. Passing in a value of `canceled` will return all canceled subscriptions, including those belonging to deleted customers. Pass `ended` to find subscriptions that are canceled and subscriptions that are expired due to [incomplete payment](https://stripe.com/docs/billing/subscriptions/overview#subscription-statuses). Passing in a value of `all` will return subscriptions of all statuses. If no value is supplied, all subscriptions that have not been canceled are returned.>
    void take
  take get-subscriptions-body, like get-subscriptions-body
    void take

  line </v1/subscriptions>

  find <collection_method>, loan collection-method
  find <created>, loan created
  find <current_period_end>, loan current-period-end
  find <current_period_start>, loan current-period-start
  find <customer>, loan customer
  find <ending_before>, loan ending-before
  find <expand>, loan expand
  find <limit>, loan limit
  find <price>, loan price
  find <starting_after>, loan starting-after
  find <status>, loan status

  seed x-www-form-urlencoded, loan get-subscriptions-body

  loot 200
    seed json
      like form
        take data, name <data>
        take has-more, name <has_more>
        take object, name <object>
        take url, name <url>
    note <Successful response.>
  loot fall
    seed json, like error
    note <Error response.>
