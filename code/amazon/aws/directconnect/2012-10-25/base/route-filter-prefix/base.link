
load @textsurf/snow/amazonaws.com/directconnect/2012-10-25/base/cidr
  take form cidr

form route-filter-prefix, name <RouteFilterPrefix>
  note <Information about a route filter prefix that a customer can advertise through Border Gateway Protocol (BGP) over a public virtual interface.>
  take cidr, name <cidr>
    like cidr
    void take
    note <The CIDR block for the advertised route. Separate multiple routes using commas. An IPv6 CIDR must use /64 or shorter.>