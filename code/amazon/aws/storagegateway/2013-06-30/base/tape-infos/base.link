
load @wavebond/snow/amazonaws.com/storagegateway/2013-06-30/base/tape-info
  take form tape-info

load @wavebond/snow/base/list
  take form list

form tape-infos, name <TapeInfos>
  note <An array of TapeInfo objects, where each object describes a single tape. If there are no tapes in the tape library or VTS, then the `TapeInfos` is an empty array.>
  like list
    like tape-info