
load @drumwork/snow/base/native-string
  take form native-string

form role, name <Role>
  note <The ARN of the IAM role that an S3 File Gateway assumes when it accesses the underlying storage.>
  like native-string