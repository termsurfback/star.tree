
load @treesurf/snow/amazonaws.com/forecast/2018-06-26/base/s3-config
  take form s3-config

form data-destination, name <DataDestination>
  note <The destination for an export job. Provide an S3 path, an AWS Identity and Access Management (IAM) role that allows Amazon Forecast to access the location, and an AWS Key Management Service (KMS) key (optional).>
  take s3-config, name <S3Config>
    like s3-config
    note <The path to an Amazon Simple Storage Service (Amazon S3) bucket along with the credentials to access the bucket.>