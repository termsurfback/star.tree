
load @wavebond/snow/amazonaws.com/sagemaker/2017-07-24/base/processing-cluster-config
  take form processing-cluster-config

form processing-resources, name <ProcessingResources>
  note <Identifies the resources, ML compute instances, and ML storage volumes to deploy for a processing job. In distributed training, you specify more than one instance.>
  take cluster-config, name <ClusterConfig>
    like processing-cluster-config
    note <The configuration for the resources in a cluster used to run the processing job.>
