
form s3-delete-object-tagging-operation, name <S3DeleteObjectTaggingOperation>
  note <Contains no configuration parameters because the DELETE Object tagging API only accepts the bucket name and key name as parameters, which are defined in the job's manifest.>