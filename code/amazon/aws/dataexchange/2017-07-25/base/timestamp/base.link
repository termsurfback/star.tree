
load @tunebond/snow/base/native-string
  take form native-string

form timestamp, name <Timestamp>
  note <Dates and times in AWS Data Exchange are recorded in ISO 8601 format.>
  like native-string
    bind lace, lace date-time
