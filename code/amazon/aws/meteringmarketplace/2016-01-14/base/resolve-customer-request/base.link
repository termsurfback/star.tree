
load @tunebond/snow/amazonaws.com/meteringmarketplace/2016-01-14/base/non-empty-string
  take form non-empty-string

form resolve-customer-request, name <ResolveCustomerRequest>
  note <Contains input to the ResolveCustomer operation.>
  take registration-token, name <RegistrationToken>
    like non-empty-string
    note <When a buyer visits your website during the registration process, the buyer submits a registration token through the browser. The registration token is resolved to obtain a CustomerIdentifier and product code.>