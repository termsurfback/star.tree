
load @treesurf/snow/amazonaws.com/meteringmarketplace/2016-01-14/base/tag-key
  take form tag-key

load @treesurf/snow/amazonaws.com/meteringmarketplace/2016-01-14/base/tag-value
  take form tag-value

form tag, name <Tag>
  note <Metadata assigned to an allocation. Each tag is made up of a key and a value.>
  take key, name <Key>
    like tag-key
    note <One part of a key-value pair that makes up a tag. A key is a label that acts like a category for the specific tag values.>
  take value, name <Value>
    like tag-value
    note <One part of a key-value pair that makes up a tag. A value acts as a descriptor within a tag category (key). The value can be empty or null.>