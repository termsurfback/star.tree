
load @wavebond/snow/base/native-string
  take form native-string

form s3-prefix, name <S3Prefix>
  note <The prefix that AWS adds to the report name when AWS delivers the report. Your prefix can't include spaces.>
  like native-string