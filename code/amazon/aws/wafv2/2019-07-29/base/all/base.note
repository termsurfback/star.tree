
form all, name <All>
  note <Inspect all of the elements that WAF has parsed and extracted from the web request JSON body that are within the JsonBody `MatchScope`. This is used with the FieldToMatch option `JsonBody`.

This is used only to indicate the web request component for WAF to inspect, in the FieldToMatch specification.

JSON specification: `"All": {}`>