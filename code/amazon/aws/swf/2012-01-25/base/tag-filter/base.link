
load @wavebond/snow/amazonaws.com/swf/2012-01-25/base/tag
  take form tag

form tag-filter, name <TagFilter>
  note <Used to filter the workflow executions in visibility APIs based on a tag.>
  take tag, name <tag>
    like tag
    note <Specifies the tag that must be associated with the execution for it to meet the filter criteria.

Tags may only contain unicode letters, digits, whitespace, or these symbols: `_ . : / = + - @`.>
