
load @textsurf/snow/amazonaws.com/comprehend/2017-11-27/base/entity-type-name
  take form entity-type-name

form entity-types-list-item, name <EntityTypesListItem>
  note <An entity type within a labeled training dataset that Amazon Comprehend uses to train a custom entity recognizer.>
  take type, name <Type>
    like entity-type-name
    note <An entity type within a labeled training dataset that Amazon Comprehend uses to train a custom entity recognizer.

Entity types must not contain the following invalid characters: \n (line break), \\n (escaped line break, \r (carriage return), \\r (escaped carriage return), \t (tab), \\t (escaped tab), space, and , (comma).>