
load @wavebond/snow/base/string
  take form string

form host-entry, name <HostEntry>
  note <Hostnames and IP address entries that are added to the `/etc/hosts` file of a container via the `extraHosts` parameter of its ContainerDefinition.>
  take hostname, name <hostname>
    like string
    note <The hostname to use in the `/etc/hosts` entry.>
  take ip-address, name <ipAddress>
    like string
    note <The IP address to use in the `/etc/hosts` entry.>