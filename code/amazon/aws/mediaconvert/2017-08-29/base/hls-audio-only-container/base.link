
load @nerdbond/snow/base/native-string
  take form native-string

form hls-audio-only-container, name <HlsAudioOnlyContainer>
  note <Use this setting only in audio-only outputs. Choose MPEG-2 Transport Stream (M2TS) to create a file in an MPEG2-TS container. Keep the default value Automatic (AUTOMATIC) to create a raw audio-only file with no container. Regardless of the value that you specify here, if this output has video, the service will place outputs into an MPEG2-TS container.>
  like native-string
    text <AUTOMATIC>
    text <M2TS>