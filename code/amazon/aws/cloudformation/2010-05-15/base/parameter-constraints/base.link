
load @treesurf/snow/amazonaws.com/cloudformation/2010-05-15/base/allowed-values
  take form allowed-values

form parameter-constraints, name <ParameterConstraints>
  note <A set of criteria that CloudFormation uses to validate parameter values. Although other constraints might be defined in the stack template, CloudFormation returns only the `AllowedValues` property.>
  take allowed-values, name <AllowedValues>
    like allowed-values
    void take
    note <A list of values that are permitted for a parameter.>