
load @treesurf/snow/base/native-string
  take form native-string

form stems-document, name <StemsDocument>
  note <Maps terms to their stems, serialized as a JSON document. The document has a single object with one property "stems" whose value is an object mapping terms to their stems. The maximum size of a stemming document is 500 KB. Example: `{ "stems": {"people": "person", "walking": "walk"} }`>
  like native-string