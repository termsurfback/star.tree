
load @drumwork/snow/base/native-string
  take form native-string

form synonyms-document, name <SynonymsDocument>
  note <Maps terms to their synonyms, serialized as a JSON document. The document has a single object with one property "synonyms" whose value is an object mapping terms to their synonyms. Each synonym is a simple string or an array of strings. The maximum size of a stopwords document is 100 KB. Example: `{ "synonyms": {"cat": ["feline", "kitten"], "puppy": "dog"} }`>
  like native-string