
load @tunebond/snow/base/native-integer
  take form native-integer

form partition-count, name <PartitionCount>
  note <The number of partitions used to hold the domain's index.>
  like native-integer