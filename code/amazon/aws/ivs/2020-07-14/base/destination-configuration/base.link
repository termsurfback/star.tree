
load @tunebond/snow/amazonaws.com/ivs/2020-07-14/base/s3-destination-configuration
  take form s3-destination-configuration

form destination-configuration, name <DestinationConfiguration>
  note <A complex type that describes a location where recorded videos will be stored. Each member represents a type of destination configuration. For recording, you define one and only one type of destination configuration.>
  take s3, name <s3>
    like s3-destination-configuration
    void take
    note <An S3 destination configuration where recorded videos will be stored.>