
load @tunebond/snow/base/native-string
  take form native-string

form daily-time, name <DailyTime>
  note <A recurring daily time, in the format `HH:MM`. `HH` is the zero-padded hour of the day (0-23), and `MM` is the zero-padded minute of the hour. For example, `05:00` specifies 5 AM daily.>
  like native-string