
load @nerdbond/snow/base/native-string
  take form native-string

form next-token, name <NextToken>
  note <(Optional) Opaque pagination token returned from a previous operation (String). If present, this token indicates from what point you can continue processing the request, where the previous `NextToken` value left off.>
  like native-string