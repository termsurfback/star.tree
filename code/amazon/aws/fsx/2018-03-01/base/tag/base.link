
load @termsurf/snow/amazonaws.com/fsx/2018-03-01/base/tag-key
  take form tag-key

load @termsurf/snow/amazonaws.com/fsx/2018-03-01/base/tag-value
  take form tag-value

form tag, name <Tag>
  note <Specifies a key-value pair for a resource tag.>
  take key, name <Key>
    like tag-key
    note <A value that specifies the `TagKey`, the name of the tag. Tag keys must be unique for the resource to which they are attached.>
  take value, name <Value>
    like tag-value
    note <A value that specifies the `TagValue`, the value assigned to the corresponding tag key. Tag values can be null and don't have to be unique in a tag set. For example, you can have a key-value pair in a tag set of `finances : April` and also of `payroll : April`.>