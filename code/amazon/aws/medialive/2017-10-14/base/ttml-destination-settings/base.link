
load @termsurf/snow/amazonaws.com/medialive/2017-10-14/base/ttml-destination-style-control
  take form ttml-destination-style-control

form ttml-destination-settings, name <TtmlDestinationSettings>
  note <Ttml Destination Settings>
  take style-control, name <StyleControl>
    like ttml-destination-style-control
    void take
    note <When set to passthrough, passes through style and position information from a TTML-like input source (TTML, SMPTE-TT, CFF-TT) to the CFF-TT output or TTML output.>
    host xml
      host name, text <styleControl>