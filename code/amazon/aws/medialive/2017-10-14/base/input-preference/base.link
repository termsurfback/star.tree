
load @textsurf/snow/base/native-string
  take form native-string

form input-preference, name <InputPreference>
  note <Input preference when deciding which input to make active when a previously failed input has recovered. If \"EQUAL_INPUT_PREFERENCE\", then the active input will stay active as long as it is healthy. If \"PRIMARY_INPUT_PREFERRED\", then always switch back to the primary input when it is healthy.>
  like native-string
    text <EQUAL_INPUT_PREFERENCE>
    text <PRIMARY_INPUT_PREFERRED>