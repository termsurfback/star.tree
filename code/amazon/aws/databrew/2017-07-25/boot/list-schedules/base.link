
load @tunebond/snow/amazonaws.com/databrew/2017-07-25/base/list-schedules-response
  take form list-schedules-response

load @tunebond/snow/amazonaws.com/databrew/2017-07-25/base/validation-exception
  take form validation-exception

load @tunebond/snow/base/native-integer
  take form native-integer

load @tunebond/snow/base/native-string
  take form native-string

boot list-schedules, name <ListSchedules>
  deed get
  note <Lists the DataBrew schedules that are defined.>

  take job-name
    like native-string
    note <The name of the job that these schedules apply to.>
    void take
  take max-results
    like native-integer
    note <The maximum number of results to return in this request.>
    void take
  take next-token
    like native-string
    note <The token returned by a previous call to retrieve the next set of results.>
    void take
  take max-results
    like native-string
    note <Pagination limit>
    void take
  take next-token
    like native-string
    note <Pagination token>
    void take

  line </schedules>

  find <jobName>, loan job-name
  find <maxResults>, loan max-results
  find <nextToken>, loan next-token
  find <MaxResults>, loan max-results
  find <NextToken>, loan next-token

  loot 200
    seed json, like list-schedules-response
    note <Success>
  loot 480
    seed json, like validation-exception
    note <ValidationException>