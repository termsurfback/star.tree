
load @drumwork/snow/amazonaws.com/databrew/2017-07-25/base/list-jobs-response
  take form list-jobs-response

load @drumwork/snow/amazonaws.com/databrew/2017-07-25/base/validation-exception
  take form validation-exception

load @drumwork/snow/base/native-integer
  take form native-integer

load @drumwork/snow/base/native-string
  take form native-string

boot list-jobs, name <ListJobs>
  deed get
  note <Lists all of the DataBrew jobs that are defined.>

  take dataset-name
    like native-string
    note <The name of a dataset. Using this parameter indicates to return only those jobs that act on the specified dataset.>
    void take
  take max-results
    like native-integer
    note <The maximum number of results to return in this request.>
    void take
  take next-token
    like native-string
    note <A token generated by DataBrew that specifies where to continue pagination if a previous request was truncated. To get the next set of pages, pass in the NextToken value from the response object of the previous page call.>
    void take
  take project-name
    like native-string
    note <The name of a project. Using this parameter indicates to return only those jobs that are associated with the specified project.>
    void take
  take max-results
    like native-string
    note <Pagination limit>
    void take
  take next-token
    like native-string
    note <Pagination token>
    void take

  line </jobs>

  find <datasetName>, loan dataset-name
  find <maxResults>, loan max-results
  find <nextToken>, loan next-token
  find <projectName>, loan project-name
  find <MaxResults>, loan max-results
  find <NextToken>, loan next-token

  loot 200
    seed json, like list-jobs-response
    note <Success>
  loot 480
    seed json, like validation-exception
    note <ValidationException>