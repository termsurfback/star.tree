
load @nerdbond/snow/amazonaws.com/models.lex.v2/2020-08-07/base/intent-filters
  take form intent-filters

load @nerdbond/snow/amazonaws.com/models.lex.v2/2020-08-07/base/intent-sort-by
  take form intent-sort-by

load @nerdbond/snow/amazonaws.com/models.lex.v2/2020-08-07/base/max-results
  take form max-results

load @nerdbond/snow/amazonaws.com/models.lex.v2/2020-08-07/base/next-token
  take form next-token

form list-intents-request, name <ListIntentsRequest>
  take sort-by, name <sortBy>
    like intent-sort-by
    void take
    note <Determines the sort order for the response from the `ListIntents` operation. You can choose to sort by the intent name or last updated date in either ascending or descending order.>
  take filters, name <filters>
    like intent-filters
    void take
    note <Provides the specification of a filter used to limit the intents in the response to only those that match the filter specification. You can only specify one filter and only one string to filter on.>
  take max-results, name <maxResults>
    like max-results
    void take
    note <The maximum number of intents to return in each page of results. If there are fewer results than the max page size, only the actual number of results are returned.>
  take next-token, name <nextToken>
    like next-token
    void take
    note <If the response from the `ListIntents` operation contains more results than specified in the `maxResults` parameter, a token is returned in the response. Use that token in the `nextToken` parameter to return the next page of results.>