
load @drumwork/snow/base/boolean
  take form boolean

form dialog-code-hook-settings, name <DialogCodeHookSettings>
  note <Settings that determine the Lambda function that Amazon Lex uses for processing user responses.>
  take enabled, name <enabled>
    like boolean
    note <Enables the dialog code hook so that it processes user requests.>