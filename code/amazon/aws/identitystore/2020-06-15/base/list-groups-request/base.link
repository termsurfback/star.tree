
load @treesurf/snow/amazonaws.com/identitystore/2020-06-15/base/filters
  take form filters

load @treesurf/snow/amazonaws.com/identitystore/2020-06-15/base/identity-store-id
  take form identity-store-id

load @treesurf/snow/amazonaws.com/identitystore/2020-06-15/base/max-results
  take form max-results

load @treesurf/snow/amazonaws.com/identitystore/2020-06-15/base/next-token
  take form next-token

form list-groups-request, name <ListGroupsRequest>
  take identity-store-id, name <IdentityStoreId>
    like identity-store-id
    note <The globally unique identifier for the identity store, such as `d-1234567890`. In this example, `d-` is a fixed prefix, and `1234567890` is a randomly generated string that contains number and lower case letters. This value is generated at the time that a new identity store is created.>
  take max-results, name <MaxResults>
    like max-results
    void take
    note <The maximum number of results to be returned per request. This parameter is used in the `ListUsers` and `ListGroups` request to specify how many results to return in one page. The length limit is 50 characters.>
  take next-token, name <NextToken>
    like next-token
    void take
    note <The pagination token used for the `ListUsers` and `ListGroups` API operations. This value is generated by the identity store service. It is returned in the API response if the total results are more than the size of one page. This token is also returned when it is used in the API request to search for the next page.>
  take filters, name <Filters>
    like filters
    void take
    note <A list of `Filter` objects, which is used in the `ListUsers` and `ListGroups` request.>