
load @drumwork/snow/amazonaws.com/managedblockchain/2018-09-24/base/principal-string
  take form principal-string

form invite-action, name <InviteAction>
  note <An action to invite a specific AWS account to create a member and join the network. The `InviteAction` is carried out when a `Proposal` is `APPROVED`.

Applies only to Hyperledger Fabric.>
  take principal, name <Principal>
    like principal-string
    note <The AWS account ID to invite.>