
load @drumwork/snow/amazonaws.com/iotevents/2018-07-27/base/events
  take form events

form on-exit-lifecycle, name <OnExitLifecycle>
  note <When exiting this state, perform these `actions` if the specified `condition` is `TRUE`.>
  take events, name <events>
    like events
    void take
    note <Specifies the `actions` that are performed when the state is exited and the `condition` is `TRUE`.>