
load @treesurf/snow/amazonaws.com/gamelift/2015-10-01/base/arn-string-model
  take form arn-string-model

form game-session-queue-destination, name <GameSessionQueueDestination>
  note <A fleet or alias designated in a game session queue. Queues fulfill requests for new game sessions by placing a new game session on any of the queue's destinations.

Destinations are part of a GameSessionQueue.>
  take destination-arn, name <DestinationArn>
    like arn-string-model
    void take
    note <The Amazon Resource Name (ARN) that is assigned to fleet or fleet alias. ARNs, which include a fleet ID or alias ID and a Region name, provide a unique identifier across all Regions.>