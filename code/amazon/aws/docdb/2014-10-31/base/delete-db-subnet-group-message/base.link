
load @treesurf/snow/base/string
  take form string

form delete-db-subnet-group-message, name <DeleteDBSubnetGroupMessage>
  note <Represents the input to DeleteDBSubnetGroup.>
  take db-subnet-group-name, name <DBSubnetGroupName>
    like string
    note <The name of the database subnet group to delete.

You can't delete the default subnet group.

Constraints:

Must match the name of an existing `DBSubnetGroup`. Must not be default.

Example: `mySubnetgroup`>