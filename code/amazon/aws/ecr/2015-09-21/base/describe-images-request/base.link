
load @treesurf/snow/amazonaws.com/ecr/2015-09-21/base/describe-images-filter
  take form describe-images-filter

load @treesurf/snow/amazonaws.com/ecr/2015-09-21/base/image-identifier-list
  take form image-identifier-list

load @treesurf/snow/amazonaws.com/ecr/2015-09-21/base/max-results
  take form max-results

load @treesurf/snow/amazonaws.com/ecr/2015-09-21/base/next-token
  take form next-token

load @treesurf/snow/amazonaws.com/ecr/2015-09-21/base/registry-id
  take form registry-id

load @treesurf/snow/amazonaws.com/ecr/2015-09-21/base/repository-name
  take form repository-name

form describe-images-request, name <DescribeImagesRequest>
  take registry-id, name <registryId>
    like registry-id
    void take
    note <The Amazon Web Services account ID associated with the registry that contains the repository in which to describe images. If you do not specify a registry, the default registry is assumed.>
  take repository-name, name <repositoryName>
    like repository-name
    note <The repository that contains the images to describe.>
  take image-ids, name <imageIds>
    like image-identifier-list
    void take
    note <The list of image IDs for the requested repository.>
  take next-token, name <nextToken>
    like next-token
    void take
    note <The `nextToken` value returned from a previous paginated `DescribeImages` request where `maxResults` was used and the results exceeded the value of that parameter. Pagination continues from the end of the previous results that returned the `nextToken` value. This value is `null` when there are no more results to return. This option cannot be used when you specify images with `imageIds`.>
  take max-results, name <maxResults>
    like max-results
    void take
    note <The maximum number of repository results returned by `DescribeImages` in paginated output. When this parameter is used, `DescribeImages` only returns `maxResults` results in a single page along with a `nextToken` response element. The remaining results of the initial request can be seen by sending another `DescribeImages` request with the returned `nextToken` value. This value can be between 1 and 1000. If this parameter is not used, then `DescribeImages` returns up to 100 results and a `nextToken` value, if applicable. This option cannot be used when you specify images with `imageIds`.>
  take filter, name <filter>
    like describe-images-filter
    void take
    note <The filter key and value with which to filter your `DescribeImages` results.>