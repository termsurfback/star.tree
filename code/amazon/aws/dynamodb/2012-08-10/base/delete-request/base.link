
load @tunebond/snow/amazonaws.com/dynamodb/2012-08-10/base/key
  take form key

form delete-request, name <DeleteRequest>
  note <Represents a request to perform a `DeleteItem` operation on an item.>
  take key, name <Key>
    like key
    note <A map of attribute name to attribute values, representing the primary key of the item to delete. All of the table's primary key attributes must be specified, and their data types must match those of the table's key schema.>