
load @textsurf/snow/amazonaws.com/dynamodb/2011-12-05/base/attribute-value
  take form attribute-value

load @textsurf/snow/base/list
  take form list

form attribute-value-list, name <AttributeValueList>
  note <A list of attribute values to be used with a comparison operator for a scan or query operation. For comparisons that require more than one value, such as a `BETWEEN` comparison, the AttributeValueList contains two attribute values and the comparison operator.>
  like list
    like attribute-value