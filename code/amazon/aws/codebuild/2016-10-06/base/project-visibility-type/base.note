
load @termsurf/snow/base/native-string
  take form native-string

form project-visibility-type, name <ProjectVisibilityType>
  note <Specifies the visibility of the project's builds. Possible values are:

PUBLIC_READ

The project builds are visible to the public.

PRIVATE

The project builds are not visible to the public.>
  like native-string
    text <PUBLIC_READ>
    text <PRIVATE>