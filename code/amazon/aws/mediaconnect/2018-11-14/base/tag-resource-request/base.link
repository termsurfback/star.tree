
load @treesurf/snow/amazonaws.com/mediaconnect/2018-11-14/base/map-of-string
  take form map-of-string

form tag-resource-request, name <TagResourceRequest>
  note <The tags to add to the resource. A tag is an array of key-value pairs. Tag keys can have a maximum character length of 128 characters, and tag values can have a maximum length of 256 characters.>
  take tags, name <Tags>
    like map-of-string
    note <A map from tag keys to values. Tag keys can have a maximum character length of 128 characters, and tag values can have a maximum length of 256 characters.>
    host xml
      host name, text <tags>