
load @textsurf/snow/amazonaws.com/securityhub/2018-10-26/base/describe-standards-response
  take form describe-standards-response

load @textsurf/snow/amazonaws.com/securityhub/2018-10-26/base/internal-exception
  take form internal-exception

load @textsurf/snow/amazonaws.com/securityhub/2018-10-26/base/invalid-access-exception
  take form invalid-access-exception

load @textsurf/snow/amazonaws.com/securityhub/2018-10-26/base/invalid-input-exception
  take form invalid-input-exception

load @textsurf/snow/base/native-integer
  take form native-integer

load @textsurf/snow/base/native-string
  take form native-string

boot describe-standards, name <DescribeStandards>
  deed get
  note <Returns a list of the available standards in Security Hub.

For each standard, the results include the standard ARN, the name, and a description.>

  take next-token
    like native-string
    note <The token that is required for pagination. On your first call to the `DescribeStandards` operation, set the value of this parameter to `NULL`.

For subsequent calls to the operation, to continue listing data, set the value of this parameter to the value returned from the previous response.>
    void take
  take max-results
    like native-integer
    note <The maximum number of standards to return.>
    void take

  line </standards>

  find <NextToken>, loan next-token
  find <MaxResults>, loan max-results

  loot 200
    seed json, like describe-standards-response
    note <Success>
  loot 480
    seed json, like internal-exception
    note <InternalException>
  loot 481
    seed json, like invalid-input-exception
    note <InvalidInputException>
  loot 482
    seed json, like invalid-access-exception
    note <InvalidAccessException>