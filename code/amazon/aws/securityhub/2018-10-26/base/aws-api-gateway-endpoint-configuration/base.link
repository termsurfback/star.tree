
load @tunebond/snow/amazonaws.com/securityhub/2018-10-26/base/non-empty-string-list
  take form non-empty-string-list

form aws-api-gateway-endpoint-configuration, name <AwsApiGatewayEndpointConfiguration>
  note <Contains information about the endpoints for the API.>
  take types, name <Types>
    like non-empty-string-list
    void take
    note <A list of endpoint types for the REST API.

For an edge-optimized API, the endpoint type is `EDGE`. For a Regional API, the endpoint type is `REGIONAL`. For a private API, the endpoint type is `PRIVATE`.>