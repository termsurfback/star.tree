
load @nerdbond/snow/amazonaws.com/securityhub/2018-10-26/base/long
  take form long

form range, name <Range>
  note <Identifies where the sensitive data begins and ends.>
  take start, name <Start>
    like long
    void take
    note <The number of lines (for a line range) or characters (for an offset range) from the beginning of the file to the end of the sensitive data.>
  take end, name <End>
    like long
    void take
    note <The number of lines (for a line range) or characters (for an offset range) from the beginning of the file to the end of the sensitive data.>
  take start-column, name <StartColumn>
    like long
    void take
    note <In the line where the sensitive data starts, the column within the line where the sensitive data starts.>