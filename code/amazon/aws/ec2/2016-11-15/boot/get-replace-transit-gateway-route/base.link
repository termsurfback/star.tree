
load @wavebond/snow/amazonaws.com/ec2/2016-11-15/base/replace-transit-gateway-route-result
  take form replace-transit-gateway-route-result

load @wavebond/snow/base/native-boolean
  take form native-boolean

load @wavebond/snow/base/native-string
  take form native-string

boot get-replace-transit-gateway-route, name <GET_ReplaceTransitGatewayRoute>
  deed get
  note <Replaces the specified route in the specified transit gateway route table.>

  take destination-cidr-block
    like native-string
    note <The CIDR range used for the destination match. Routing decisions are based on the most specific match.>
  take transit-gateway-route-table-id
    like native-string
    note <The ID of the route table.>
  take transit-gateway-attachment-id
    like native-string
    note <The ID of the attachment.>
    void take
  take blackhole
    like native-boolean
    note <Indicates whether traffic matching this route is to be dropped.>
    void take
  take dry-run
    like native-boolean
    note <Checks whether you have the required permissions for the action, without actually making the request, and provides an error response. If you have the required permissions, the error response is `DryRunOperation`. Otherwise, it is `UnauthorizedOperation`.>
    void take
  take action
    like native-string
  take version
    like native-string

  line </#Action=ReplaceTransitGatewayRoute>

  find <DestinationCidrBlock>, loan destination-cidr-block
  find <TransitGatewayRouteTableId>, loan transit-gateway-route-table-id
  find <TransitGatewayAttachmentId>, loan transit-gateway-attachment-id
  find <Blackhole>, loan blackhole
  find <DryRun>, loan dry-run
  find <Action>, loan action
  find <Version>, loan version

  loot 200
    seed xml, like replace-transit-gateway-route-result
    note <Success>
