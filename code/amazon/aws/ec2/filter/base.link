state affinity
  draft |The affinity setting for an instance running on a Dedicated Host (default | host).|

state architecture
  draft |The instance architecture (i386 | x86_64 | arm64).|

state availability-zone
  draft |The Availability Zone of the instance.|

state block-device-mapping.attach-time
  draft |The attach time for an EBS volume mapped to the instance, for example, 2010-09-15T17:15:20.000Z.|

state block-device-mapping.delete-on-termination
  draft |A Boolean that indicates whether the EBS volume is deleted on instance termination.|

state block-device-mapping.device-name
  draft |The device name specified in the block device mapping (for example, /dev/sdh or xvdh).|

state block-device-mapping.status
  draft |The status for the EBS volume (attaching | attached | detaching | detached).|

state block-device-mapping.volume-id
  draft |The volume ID of the EBS volume.|

state client-token
  draft |The idempotency token you provided when you launched the instance.|

state dns-name
  draft |The public DNS name of the instance.|

state group-id
  draft |The ID of the security group for the instance. EC2-Classic only.|

state group-name
  draft |The name of the security group for the instance. EC2-Classic only.|

state hibernation-options.configured
  draft |A Boolean that indicates whether the instance is enabled for hibernation. A value of true means that the instance is enabled for hibernation.|

state host-id
  draft |The ID of the Dedicated Host on which the instance is running, if applicable.|

state hypervisor
  draft |The hypervisor type of the instance (ovm | xen). The value xen is used for both Xen and Nitro hypervisors.|

state iam-instance-profile.arn
  draft |The instance profile associated with the instance. Specified as an ARN.|

state image-id
  draft |The ID of the image used to launch the instance.|

state instance-id
  draft |The ID of the instance.|

state instance-lifecycle
  draft |Indicates whether this is a Spot Instance or a Scheduled Instance (spot | scheduled).|

state instance-state-code
  draft |The state of the instance, as a 16-bit unsigned integer. The high byte is used for internal purposes and should be ignored. The low byte is set based on the state represented. The valid values are: 0 (pending), 16 (running), 32 (shutting-down), 48 (terminated), 64 (stopping), and 80 (stopped).|

state instance-state-name
  draft |The state of the instance (pending | running | shutting-down | terminated | stopping | stopped).|

state instance-type
  draft |The type of instance (for example, t2.micro).|

state instance.group-id
  draft |The ID of the security group for the instance.|

state instance.group-name
  draft |The name of the security group for the instance.|

state ip-address
  draft |The public IPv4 address of the instance.|

state kernel-id
  draft |The kernel ID.|

state key-name
  draft |The name of the key pair used when the instance was launched.|

state launch-index
  draft |When launching multiple instances, this is the index for the instance in the launch group (for example, 0, 1, 2, and so on).|

state launch-time
  draft |The time when the instance was launched.|

state metadata-options.http-tokens
  draft |The metadata request authorization state (optional | required)|

state metadata-options.http-put-response-hop-limit
  draft |The http metadata request put response hop limit (integer, possible values 1 to 64)|

state metadata-options.http-endpoint
  draft |Enable or disable metadata access on http endpoint (enabled | disabled)|

state monitoring-state
  draft |Indicates whether detailed monitoring is enabled (disabled | enabled).|

state network-interface.addresses.private-ip-address
  draft |The private IPv4 address associated with the network interface.|

state network-interface.addresses.primary
  draft |Specifies whether the IPv4 address of the network interface is the primary private IPv4 address.|

state network-interface.addresses.association.public-ip
  draft |The ID of the association of an Elastic IP address (IPv4) with a network interface.|

state network-interface.addresses.association.ip-owner-id
  draft |The owner ID of the private IPv4 address associated with the network interface.|

state network-interface.association.public-ip
  draft |The address of the Elastic IP address (IPv4) bound to the network interface.|

state network-interface.association.ip-owner-id
  draft |The owner of the Elastic IP address (IPv4) associated with the network interface.|

state network-interface.association.allocation-id
  draft |The allocation ID returned when you allocated the Elastic IP address (IPv4) for your network interface.|

state network-interface.association.association-id
  draft |The association ID returned when the network interface was associated with an IPv4 address.|

state network-interface.attachment.attachment-id
  draft |The ID of the interface attachment.|

state network-interface.attachment.instance-id
  draft |The ID of the instance to which the network interface is attached.|

state network-interface.attachment.instance-owner-id
  draft |The owner ID of the instance to which the network interface is attached.|

state network-interface.attachment.device-index
  draft |The device index to which the network interface is attached.|

state network-interface.attachment.status
  draft |The status of the attachment (attaching | attached | detaching | detached).|

state network-interface.attachment.attach-time
  draft |The time that the network interface was attached to an instance.|

state network-interface.attachment.delete-on-termination
  draft |Specifies whether the attachment is deleted when an instance is terminated.|

state network-interface.availability-zone
  draft |The Availability Zone for the network interface.|

state network-interface.description
  draft |The description of the network interface.|

state network-interface.group-id
  draft |The ID of a security group associated with the network interface.|

state network-interface.group-name
  draft |The name of a security group associated with the network interface.|

state network-interface.ipv6-addresses.ipv6-address
  draft |The IPv6 address associated with the network interface.|

state network-interface.mac-address
  draft |The MAC address of the network interface.|

state network-interface.network-interface-id
  draft |The ID of the network interface.|

state network-interface.owner-id
  draft |The ID of the owner of the network interface.|

state network-interface.private-dns-name
  draft |The private DNS name of the network interface.|

state network-interface.requester-id
  draft |The requester ID for the network interface.|

state network-interface.requester-managed
  draft |Indicates whether the network interface is being managed by AWS.|

state network-interface.status
  draft |The status of the network interface (available) | in-use).|

state network-interface.source-dest-check
  draft |Whether the network interface performs source/destination checking. A value of true means that checking is enabled, and false means that checking is disabled. The value must be false for the network interface to perform network address translation (NAT) in your VPC.|

state network-interface.subnet-id
  draft |The ID of the subnet for the network interface.|

state network-interface.vpc-id
  draft |The ID of the VPC for the network interface.|

state owner-id
  draft |The AWS account ID of the instance owner.|

state placement-group-name
  draft |The name of the placement group for the instance.|

state placement-partition-number
  draft |The partition in which the instance is located.|

state platform
  draft |The platform. To list only Windows instances, use windows.|

state private-dns-name
  draft |The private IPv4 DNS name of the instance.|

state private-ip-address
  draft |The private IPv4 address of the instance.|

state product-code
  draft |The product code associated with the AMI used to launch the instance.|

state product-code.type
  draft |The type of product code (devpay | marketplace).|

state ramdisk-id
  draft |The RAM disk ID.|

state reason
  draft |The reason for the current state of the instance (for example, shows "User Initiated [date]" when you stop or terminate the instance). Similar to the state-reason-code filter.|

state requester-id
  draft |The ID of the entity that launched the instance on your behalf (for example, AWS Management Console, Auto Scaling, and so on).|

state reservation-id
  draft |The ID of the instance's reservation. A reservation ID is created any time you launch an instance. A reservation ID has a one-to-one relationship with an instance launch request, but can be associated with more than one instance if you launch multiple instances using the same launch request. For example, if you launch one instance, you get one reservation ID. If you launch ten instances using the same launch request, you also get one reservation ID.|

state root-device-name
  draft |The device name of the root device volume (for example, /dev/sda1).|

state root-device-type
  draft |The type of the root device volume (ebs | instance-store).|

state source-dest-check
  draft |Indicates whether the instance performs source/destination checking. A value of true means that checking is enabled, and false means that checking is disabled. The value must be false for the instance to perform network address translation (NAT) in your VPC.|

state spot-instance-request-id
  draft |The ID of the Spot Instance request.|

state state-reason-code
  draft |The reason code for the state change.|

state state-reason-message
  draft |A message that describes the state change.|

state subnet-id
  draft |The ID of the subnet for the instance.|

state tag:<key>
  draft |The key/value combination of a tag assigned to the resource. Use the tag key in the filter name and the tag value as the filter value. For example, to find all resources that have a tag with the key Owner and the value TeamA, specify tag:Owner for the filter name and TeamA for the filter value.|

state tag-key
  draft |The key of a tag assigned to the resource. Use this filter to find all resources that have a tag with a specific key, regardless of the tag value.|

state tenancy
  draft |The tenancy of an instance (dedicated | default | host).|

state virtualization-type
  draft |The virtualization type of the instance (paravirtual | hvm).|

state vpc-id
  draft |The ID of the VPC that the instance is running in.|
