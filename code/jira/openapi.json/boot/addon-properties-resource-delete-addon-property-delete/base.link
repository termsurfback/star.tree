
load @drumwork/snow/base/native-string
  take form native-string

load @drumwork/snow/jira/openapi.json/base/operation-message
  take form operation-message

boot addon-properties-resource-delete-addon-property-delete, name <AddonPropertiesResource.deleteAddonProperty_delete>
  deed delete
  note <Deletes an app's property. **[Permissions](#permissions) required:** Only a Connect app whose key matches `addonKey` can make this request.>

  take addon-key
    like native-string
    note <The key of the app, as defined in its descriptor.>
  take property-key
    like native-string
    note <The key of the property.>

  line </rest/atlassian-connect/1/addons/{addon-key}/properties/{property-key}>

  loot 204
    note <Returned if the request is successful.>
  loot 400
    seed json, like operation-message
    note <Returned if the property key is longer than 127 characters.>
  loot 401
    seed json, like operation-message
    note <Returned if the authentication credentials are incorrect or missing.>
  loot 404
    seed json, like operation-message
    note <Returned if the property is not found or doesn't belong to the app.>