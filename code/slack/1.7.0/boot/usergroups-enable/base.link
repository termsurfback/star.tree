
load @wavebond/snow/base/native-string
  take form native-string

load @wavebond/snow/slack.com/1.7.0/base/defs-ok-false
  take form defs-ok-false

load @wavebond/snow/slack.com/1.7.0/base/defs-ok-true
  take form defs-ok-true

load @wavebond/snow/slack.com/1.7.0/base/objs-subteam
  take form objs-subteam

load @wavebond/snow/slack.com/1.7.0/base/usergroups-enable-body
  take form usergroups-enable-body

boot usergroups-enable, name <usergroups_enable>
  deed post
  note <Enable a User Group>

  take token
    like native-string
    note <Authentication token. Requires scope: `usergroups:write`>
  take usergroups-enable-body, like usergroups-enable-body
    void take

  line </usergroups.enable>

  hint <token>, loan token

  seed x-www-form-urlencoded, loan usergroups-enable-body

  loot 200
    seed json
      like form
        take ok, name <ok>
          like defs-ok-true
        take usergroup, name <usergroup>
          like objs-subteam
    note <Typical success response>
  loot fall
    seed json
      like form
        take callstack, name <callstack>
          void take
        take error, name <error>
        take ok, name <ok>
          like defs-ok-false
    note <Typical error response>
