
load @textsurf/snow/base/native-string
  take form native-string

load @textsurf/snow/slack.com/1.7.0/base/defs-ok-false
  take form defs-ok-false

load @textsurf/snow/slack.com/1.7.0/base/defs-ok-true
  take form defs-ok-true

boot dnd-end-dnd, name <dnd_endDnd>
  deed post
  note <Ends the current user's Do Not Disturb session immediately.>

  take token
    like native-string
    note <Authentication token. Requires scope: `dnd:write`>

  line </dnd.endDnd>

  hint <token>, loan token

  loot 200
    seed json
      like form
        take ok, name <ok>
          like defs-ok-true
    note <Typical success response>
  loot fall
    seed json
      like form
        take callstack, name <callstack>
          void take
        take error, name <error>
        take ok, name <ok>
          like defs-ok-false
    note <Typical error response>
