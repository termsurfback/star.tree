
load @nerdbond/snow/base/native-string
  take form native-string

load @nerdbond/snow/slack.com/1.7.0/base/defs-ok-false
  take form defs-ok-false

load @nerdbond/snow/slack.com/1.7.0/base/defs-ok-true
  take form defs-ok-true

load @nerdbond/snow/slack.com/1.7.0/base/pins-add-body
  take form pins-add-body

boot pins-add, name <pins_add>
  deed post
  note <Pins an item to a channel.>

  take token
    like native-string
    note <Authentication token. Requires scope: `pins:write`>
  take pins-add-body, like pins-add-body

  line </pins.add>

  hint <token>, loan token

  seed x-www-form-urlencoded, loan pins-add-body

  loot 200
    seed json
      like form
        take ok, name <ok>
          like defs-ok-true
    note <Typical success response>
  loot fall
    seed json
      like form
        take callstack, name <callstack>
          void take
        take error, name <error>
        take ok, name <ok>
          like defs-ok-false
    note <Typical error response>
