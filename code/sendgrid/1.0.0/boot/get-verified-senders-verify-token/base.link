
load @drumwork/snow/base/native-string
  take form native-string

boot get-verified-senders-verify-token, name <GET_verified_senders-verify-token>
  deed get
  note <**This endpoint allows you to verify a sender requests.** The token is generated by SendGrid and included in a verification email delivered to the address that's pending verification.>

  take token
    like native-string

  line </verified_senders/verify/{token}>

  loot 204
  loot 401
  loot 403
  loot 404
    seed json
      like form
        take errors, name <errors>
  loot 500