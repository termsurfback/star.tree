
boot get-ips-remaining, name <GET_ips-remaining>
  deed get
  note <**This endpoint gets amount of IP Addresses that can still be created during a given period and the price of those IPs.**>

  line </ips/remaining>

  loot 200
    seed json
      like form
        take results, name <results>