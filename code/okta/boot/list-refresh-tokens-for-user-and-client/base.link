
load @textsurf/snow/base/native-integer
  take form native-integer

load @textsurf/snow/base/native-string
  take form native-string

boot list-refresh-tokens-for-user-and-client, name <listRefreshTokensForUserAndClient>
  deed get
  note <Lists all refresh tokens issued for the specified User and Client.>

  take user-id
    like native-string
  take client-id
    like native-string
  take expand
    like native-string
    void take
  take after
    like native-string
    void take
  take limit
    like native-integer
      bind lace, lace int32
        fall <20>
    void take

  line </api/v1/users/{user-id}/clients/{client-id}/tokens>

  find <expand>, loan expand
  find <after>, loan after
  find <limit>, loan limit

  loot 200
    note <Success>
